# Song Configuration File
# Define the songs to download with their URLs and output directory names
# Format: Each song has a URL and a directory name where tracks will be saved

songs:
#  - url: "https://www.karaoke-version.com/song1.html"
#    name: "My_Song"
#    description: "Example song 1"
#    key: 2  # Optional: Pitch adjustment in semitones (-12 to +12)
    
  - url: "https://www.karaoke-version.com/custombackingtrack/jimmy-eat-world/the-middle.html"
    # name: "Custom_Folder_Name"  # Optional: specify custom folder name, or leave blank to auto-extract from URL
    description: "Jimmy Eat World - The Middle"
    key: -1
    
# Notes:
# - URLs should be the direct links to individual song pages
# - Name is optional - if omitted, folder name will be auto-extracted from URL as "Artist_Song"
# - Names will be used as directory names, so avoid special characters  
# - Description is optional but helpful for organization
# - Key is optional pitch adjustment in semitones (-12 to +12), defaults to 0
# - Add as many songs as needed following the same format
